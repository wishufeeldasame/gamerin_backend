# #54 계정 교체 시 이전 전적 캐시 제거

대상: https://github.com/wishufeeldasame/gamerin_backend/issues/54

## 동작

- Riot 계정의 `puuid`가 바뀌면 `connectRiot`에서 기존 `LOL` 항목을 제거한다. 새 계정의 전적 조회가 실패해도 이전 계정 전적은 복원하지 않는다.
- 같은 유효한 `puuid`로 이름·태그만 바꾸면 기존 캐시를 유지한다. 비교는 기존 정책대로 대소문자를 구분한다.
- PUBG도 기존 통계 Map을 그대로 복사하던 동일 문제가 있었다. `accountId`가 바뀌면 연결 정보만 새로 구성하고 이전 통계를 제거한다. 같은 계정의 닉네임 변경은 캐시를 유지한다.
- 기존 식별자가 누락되거나 공백이면 캐시의 소유 계정을 확인할 수 없으므로 새 연결에서 캐시를 유지하지 않는다.
- 다른 게임의 데이터는 보존하며 연결 버전은 기존처럼 증가한다.

## 저장과 실패

변경은 `UserProfile`의 기존 Map 복사 패턴을 따른다. 연결 변경과 캐시 제거는 #52의 `GameStatsPersistenceService`가 잠근 최신 프로필에서 같은 트랜잭션으로 처리한다. A의 전적 요청 중 B로 교체되어도 계정·연결 버전 검사로 A의 늦은 결과가 다시 저장되지 않는다.

외부 계정 조회 또는 중복 사전 검사가 실패하면 기존 연결·전적을 유지한다. #53의 DB 유일성 충돌로 교체가 실패하면 캐시 제거와 연결 버전까지 함께 롤백된다. API 경로·응답 DTO·외부 API 오류 계약·전적 계산은 변경하지 않는다. DB migration과 새 의존성은 없다.

## 기존 데이터

이미 잘못 저장된 `LOL` 캐시에는 원래 계정 식별자가 없어 현재 연결과 일치하는지 자동 판별할 수 없다. 이번 변경은 이후의 계정 교체를 보호하며 기존 캐시 전체를 자동 삭제하지 않는다. 기존 오염이 의심되는 데이터는 현재 계정의 정상 전적 조회로 갱신하거나, 대상·영향을 확인한 별도 승인 작업으로 초기화한다. 같은 계정 재연동은 캐시를 유지하므로 기존 오염 복구 수단으로 간주하지 않는다.

공개 프로필의 식별자 노출 정책, 외부 응답의 식별자 검증 강화, 동시 조회 요청 자체의 응답을 최신 연결에 맞추는 정책은 이번 수정 범위에 포함하지 않는다.

## 검증

- 수정 전 `UserProfileTest`에서 Riot/PUBG 기존 캐시 잔존 10건을 재현했다.
- 엔티티 테스트: 계정 교체, 누락·빈·공백 식별자, 대소문자가 다른 식별자, 동일 계정 이름 변경, 다른 게임 보존, 복사 전 Map 보존, 연결 버전 증가.
- 프로필 서비스 테스트: 공개·내 프로필에 교체 전 Riot/PUBG 통계가 노출되지 않음.
- PostgreSQL 테스트: 이미 저장된 A 캐시 제거, B 조회 실패 후 제거 상태 유지, B 조회 성공 시 새 통계 저장, 동일 계정 캐시 유지, 지연 A 응답 차단. 기존 #53 충돌 시 전체 JSON·버전 롤백 테스트도 유지한다.
- 기존 #53 운영 문서 및 README 변경은 별도 작업으로 보존한다. #54 PR에는 이 이슈의 제품·테스트·문서 변경만 포함한다.

### 실행 결과 (2026-09-24)

- Java 21.0.10, 별도 PostgreSQL 16 테스트 인스턴스에서 검증했다. 운영 DB 및 실제 게임 API는 사용하지 않았다.
- `./gradlew.bat --offline --no-daemon --init-script D:/project/tmp/issue54-verify-20260924/test.init.gradle test bootJar` 성공. 일반 테스트는 메모리 DB·임시 업로드 경로·가짜 인증 설정으로 고정하고 게임 PostgreSQL 테스트만 전용 연결을 사용했다.
- 총 545건 중 508건 통과, 37건 건너뜀, 실패·오류 0건. 건너뛴 항목은 별도 환경이 필요한 북마크 5건·해시태그 4건·알림 25건·데모 시더 2건 및 R6 실제 API 1건이다.
- 게임 PostgreSQL 동시성·회귀 42건, V21 migration 1건, V23 migration 6건은 모두 실행·통과했다. `UserProfileTest` 17건, 프로필 응답 테스트 4건, Riot 서비스 테스트 11건도 모두 통과했다.
- 이번 변경에서 테스트 20건을 추가하고 기존 지연 응답·연결 실패 테스트를 보강했다. 수정 전 실패 10건은 현재 모두 통과한다.
- 테스트 스키마가 남아 있지 않음을 확인하고 전용 DB를 종료했다. 로그·JUnit 집계는 `D:/project/tmp/issue54-verify-20260924/`에 보관했다. 로컬 검증 경로는 제품 배포 의존성이 아니다.
- 최종 diff와 트랜잭션·롤백 경로를 검토했다. 제품 변경은 `UserProfile` 한 파일이며 새 migration·의존성은 없다.
